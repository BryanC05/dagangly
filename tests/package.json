{
  "name": "umkm-marketplace-tests",
  "version": "1.0.0",
  "description": "Test scripts for UMKM Marketplace driver feature",
  "scripts": {
    "simulation": "node driver-simulation.js",
    "race-test": "node race-condition-test.js",
    "all": "npm run simulation && npm run race-test"
  },
  "dependencies": {
    "axios": "^1.6.0"
  }
}
